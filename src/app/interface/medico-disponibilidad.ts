export interface MedicoDisponibilidad {
    id_bloque : number;
    fecha : string;
    run_medico : string;
}


export interface MedicoDisponibilidad {
    medicodisponibilidades: MedicoDisponibilidad[];
}